# 后端服务开发环境Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./
COPY tsconfig.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install

# 安装tsx用于开发模式热重载
RUN npm install -g tsx

# 暴露端口
EXPOSE 3001

# 启动开发服务器（支持热重载）
CMD ["npm", "run", "dev"]
